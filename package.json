{
  "name": "modzy",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^12.0.0",
    "framer-motion": "^12.23.3",
    "lucide-react": "^0.525.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-helmet-async": "^2.0.5",
    "react-intersection-observer": "^9.16.0",
    "react-lazy-load-image-component": "^1.6.3",
    "react-router-dom": "^7.8.0",
    "react-scripts": "5.0.1",
    "swiper": "^11.2.10",
    "vanilla-tilt": "^1.8.1",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run optimize:images && react-scripts build && npm run post-build",
    "build:prod": "npm run optimize:images && set GENERATE_SOURCEMAP=false && react-scripts build && npm run post-build",
    "post-build": "echo 'Build completed successfully for cPanel hosting'",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "analyze": "npm run build && npx serve -s build",
    "check": "npm run build:prod",
    "optimize:images": "node scripts/optimize-images.js",
    "optimize:images:force": "node scripts/optimize-images.js --force"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "https://modzy.in",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
